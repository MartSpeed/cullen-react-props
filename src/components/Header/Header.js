/**NAME: Header function Component
 * DESCRIPTION: takes the information from the App client and sends it
 * through a prop instead of the raw data in the App files
 */
function Header() {
  console.log('Header function component is complete');
  return (
      <header>
        <h1>Prim Proper Props has things going on</h1>
      </header>
  )
}

export default Header;